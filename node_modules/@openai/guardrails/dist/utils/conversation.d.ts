declare const POSSIBLE_CONVERSATION_KEYS: readonly ["messages", "conversation", "conversation_history", "conversationHistory", "recent_messages", "recentMessages", "turns", "output", "outputs"];
export interface NormalizedConversationEntry {
    role?: string;
    content?: string | null;
    type?: string | null;
    tool_name?: string | null;
    arguments?: string | null;
    output?: string | null;
    call_id?: string | null;
}
/**
 * Parse conversation-like input into a flat list of message objects.
 *
 * Accepts raw JSON strings, arrays, or objects that embed conversation arrays under
 * several common keys. Returns an empty array when no conversation data is found.
 */
export declare function parseConversationInput(rawInput: unknown): unknown[];
export declare function normalizeConversation(conversation: unknown): NormalizedConversationEntry[];
export declare function appendAssistantResponse(conversation: Iterable<NormalizedConversationEntry>, llmResponse: unknown): NormalizedConversationEntry[];
export declare function mergeConversationWithItems(conversation: Iterable<NormalizedConversationEntry>, items: Iterable<unknown>): NormalizedConversationEntry[];
export { POSSIBLE_CONVERSATION_KEYS };
//# sourceMappingURL=conversation.d.ts.map