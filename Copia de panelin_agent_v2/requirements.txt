# Panelin Agent V2 - Dependencies (Pinned for Cloud Deployment)
# ==============================================================
# 
# Core dependencies for the single-agent deterministic quotation system.
# Versions pinned for reproducible builds on Cloud Run.
# Install with: pip install -r requirements.txt

# ======================
# Core API Dependencies
# ======================
fastapi==0.109.2
uvicorn[standard]==0.27.1
pydantic==2.6.1
starlette==0.36.3

# HTTP client for external API calls
httpx==0.26.0
aiohttp==3.9.3

# ======================
# LangChain Integration
# ======================
langgraph==0.2.6
langchain==0.2.16
langchain-core==0.2.38
langchain-openai==0.1.23

# ======================
# Testing (excluded in production, but needed for CI)
# ======================
pytest==8.0.0
pytest-asyncio==0.23.4

# ======================
# Development Tools (optional, for local dev)
# ======================
# black==24.1.1
# isort==5.13.2
# mypy==1.8.0

# ======================
# Cloud Run Optimizations
# ======================
# gunicorn is optional - uvicorn is sufficient for Cloud Run
# gunicorn==21.2.0

# ======================
# Observability (recommended for production)
# ======================
# opentelemetry-api==1.22.0
# opentelemetry-sdk==1.22.0
# opentelemetry-exporter-gcp-trace==1.6.0
# opentelemetry-instrumentation-fastapi==0.43b0

# ======================
# Notes:
# - Use `pip-compile requirements.in > requirements.txt` to regenerate
# - Cloud Run provides $PORT env var for the service port
# - Secrets should be injected via GCP Secret Manager
# ======================
