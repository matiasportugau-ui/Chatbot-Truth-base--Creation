# Panelin Agent V2 - Production runtime dependencies (pinned)
# ===========================================================
#
# This file is meant for reproducible production builds (e.g., Cloud Run).
# Keep it minimal and pinned. Put test/dev tooling in `requirements-dev.txt`.
#
# Install with: pip install -r requirements.txt

# API (FastAPI + Uvicorn)
fastapi==0.128.0
uvicorn==0.40.0

# Optional: sync/webhook HTTP clients (used by sync utilities if enabled)
aiohttp==3.13.3
httpx==0.28.1
