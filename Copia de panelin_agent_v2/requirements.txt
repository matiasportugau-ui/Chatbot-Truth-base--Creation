# Panelin Agent V2 - Dependencies (Pinned for Production)
# =========================================================
# 
# Core dependencies for the single-agent deterministic quotation system.
# Install with: pip install -r requirements.txt
# 
# Versions pinned for reproducible builds in Cloud Run

# Core - Always required
# ----------------------

# No external dependencies for core calculation tools!
# Python's built-in decimal module handles all precision arithmetic.

# Testing
pytest==8.0.0
pytest-asyncio==0.23.3

# LangGraph Agent
# --------------------------
langgraph==0.2.60
langchain==0.3.13
langchain-core==0.3.28
langchain-openai==0.2.14

# Optional - Alternative LLM Providers
# ------------------------------------
# langchain-anthropic==0.3.0  # For Claude models
# langchain-google-genai==2.0.4  # For Gemini models

# Optional - Vector Search (for semantic product search)
# ------------------------------------------------------
# qdrant-client==1.12.1  # Recommended: free tier, good filtering
# weaviate-client==4.9.3  # Alternative: better hybrid search

# Sync Service
# -----------------------
aiohttp==3.11.11  # For async webhook handling
httpx==0.28.1     # For Shopify API calls

# API Wrapper (Required for Cloud Run)
fastapi==0.115.6
uvicorn[standard]==0.34.0
pydantic==2.10.4
pydantic-settings==2.7.0

# Optional - Observability
# ------------------------
# langsmith==0.2.6  # LangChain observability
# langfuse==2.58.3   # Open-source alternative

# Development (not needed in production)
# -----------
# black==24.10.0
# isort==5.13.2
# mypy==1.14.1
