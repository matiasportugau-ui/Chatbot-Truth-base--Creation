01_UPLOAD

Purpose

- Holds all assets/scripts to upload with the GPT (logo, PDF generator, styles, any bundled files).

Canonical sources to pull from

- Panelin_GPT/01_UPLOAD_FILES/ (logo, pdf_generator.py, pdf_styles.py).
- panelin_reports/gpt_upload_package/ (if any additional upload-time assets are needed).

Populate

- Place final uploadable files here; if uncertain about a file, put it in 01_UPLOAD/ARCHIVE/ and document the decision in README.
