# PANELIN CONFIGURATION - TEMPLATE
# Use scripts/manage_secrets.py to set these securely in Windows Credential Manager.

# The keys below are only for local fallback if keyring is not used.
# RECOMMENDED: Keep these empty and use 'python scripts/manage_secrets.py'
OPENAI_API_KEY=
ASSISTANT_ID=

# Security for Deploy the Wolf (OpenAI Action)
WOLF_API_KEY=

# Set to true if you are using keyring (default behavior in api.py)
USE_KEYRING=true
